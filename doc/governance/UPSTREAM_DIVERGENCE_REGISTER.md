# Upstream divergence register

This register records intentional Help2day differences from public Paperclip. It is a review aid, not release approval. Every entry must stay tied to an owning issue and exact commits. Update it during each upstream reconciliation and retire entries when the divergence disappears.

| Area | Reason | Requirement | Owner issue | Relevant commits | Expected conflict area | Classification | Retirement condition |
|---|---|---|---|---|---|---|---|
| Release identity and evidence | Public upstream version metadata does not identify Help2day-governed bytes or bind exact artifacts to source evidence. | Security and release governance | AIC-132, AIC-134 | `2ab9e4694`, `64948026d`, `71ad4cdfe`, `343721777` | package manifests, lockfiles, release scripts, workflows | Help2day-specific | Upstream provides an equivalent immutable provenance, exact-artifact audit, SBOM, and installed-entrypoint contract accepted by Help2day governance. |
| Cursor SDK dependency | The previously resolved Cursor SDK transport chain included a vulnerable dependency. | Security | AIC-133 | `2248cb3a6` | Cursor adapter package metadata and lockfile | Security-required | A later supported upstream Cursor SDK line is independently verified to retain an equal or better dependency posture and is adopted upstream. |
| Premium run admission | Global premium concurrency admission must be atomic across service instances. | Cost and execution safety | AIC-130 | `ad6aabba2` | heartbeat run claiming and PostgreSQL transaction logic | Upstreamable | Equivalent atomic admission ships upstream and reconciliation proves semantic parity. |
| Execution workspace ownership and cancellation | A task must not inherit another issue's isolated workspace, and cancellation must terminate verified descendants before terminal settlement. | Workspace integrity and execution safety | AIC-085, AIC-089, AIC-093, AIC-094 | `ea535a632` | heartbeat dispatch, execution workspace reuse, local process supervision | Upstreamable | Upstream enforces issue-bound workspace ownership and identity-safe process-tree termination with equivalent adversarial tests. |
| Help2day operational governance | Source publication and production runtime activation require separate approvals and evidence. | Company governance | AIC-114, AIC-132 | Pending final reconciliation commit | release documentation, CODEOWNERS, workflows | Help2day-specific | No retirement planned unless the company governance model changes through an approved RED decision. |
| Global heartbeat concurrency cap | Decision: re-apply before the next runtime rebuild; tracked separately as the critical OOM gate. Reconciliation `b4ec00b49` dropped the global cap, which can allow unbounded concurrent local child runs even when per-agent admission is healthy. | Host stability and cost safety | FUL-16648 | Dropped in `b4ec00b49`; restore candidate tracked on FUL-16648 | heartbeat dispatch and run claiming | Help2day-required | Equivalent upstream global cap is present, tested under burst dispatch, and FUL-16648 is closed with rebuild evidence. |
| Process-loss retry ancestry cap | Decision: re-apply by wiring `shouldRetryProcessLoss` and propagating `processLossChainCount`, or delete the dead fields/module if the behavior is intentionally retired. HEAD keeps `run-retry-policy.ts` but `heartbeat.ts` still uses a resettable per-run `(processLossRetryCount ?? 0) < 1` rule. | Lifecycle loop containment | FUL-16652 | `376bd1d24`; dropped in `b4ec00b49` | `server/src/services/heartbeat.ts`, `server/src/services/recovery/service.ts`, `server/src/services/run-retry-policy.ts` | Upstreamable | Retry ancestry is capped across reaper and recovery chains, with regression tests, or all dead chain-cap exports are removed with an approved retirement rationale. |
| Run preflight and deterministic retry suppression | Decision: re-wire or delete. HEAD contains `run-preflight.ts` and tests, but the heartbeat claim/start path has zero importers, so G1/G2/G3 preflight behavior is not active and creates phantom upstream conflicts. | Agent waste prevention and deterministic failure handling | FUL-16652 | `80802c70d`; dropped in `b4ec00b49` | heartbeat claim/start path and adapter failure handling | Upstreamable | Preflight is invoked before adapter spawn and deterministic failures block/defer as designed, or the dead module/tests are deleted and the retirement is approved. |
| Quota-aware recovery quarantine | Decision: adopt upstream replacements instead of resurrecting the fork-only detector as-is. HEAD leaves `quota-error-detector.ts` mostly unimported and `providerCooldownService` only pass-through in heartbeat; the older fork path also had route-id versus adapter-type key mismatch and overly broad one-hour cooldown behavior. | Cost control and provider recovery safety | FUL-16652 | Prefer upstream `c65ab09d9`, `d32ed8844`, `555391fed`; supersedes fork commits `4e4be9c88`, `2a48fa92e`, `a67427765` | heartbeat recovery classification and provider cooldown wiring | Adopt-upstream | Upstream quota reset wait and failure-cause routing are adopted with tests; dead detector code is either integrated behind the new path or removed. |
| Standing-issue recovery skip | Decision: re-apply unless upstream lands equivalent standing-issue protection. The recovery continuation guard was dropped, while productivity-review recursion protection alone does not prevent standing issues from self-waking through recovery. | Loop prevention for standing issues | FUL-16652 | `d8ff27139`; dropped in `b4ec00b49` | `server/src/services/heartbeat.ts`, `server/src/services/recovery/service.ts`, execution policy checks | Help2day-required | Standing issues and productivity-review descendants are proven unable to spawn recovery continuation loops. |
| Assignment-recovery skip for operator-pruned cancelled runs | Decision: re-apply. Reconciliation lost the protection that prevents assignment recovery from requeueing runs intentionally cancelled/pruned by an operator. | Operator control and recovery correctness | FUL-16652 | `b416637d5`; dropped in `b4ec00b49` | `server/src/services/recovery/service.ts`, heartbeat recovery tests | Help2day-required | Cancelled/pruned runs remain terminal and assignment recovery does not recreate them unless a new explicit assignment occurs. |
| Dashboard heartbeat-list cap | Decision: re-apply before the next runtime rebuild; this is the second OOM-relevant gate. HEAD again calls `heartbeatsApi.list` without a limit on `AgentDetail`, which can request unbounded run history. | Dashboard stability and browser memory safety | FUL-16652 | `013889470`; dropped in `b4ec00b49` | `ui/src/pages/AgentDetail.tsx`, heartbeat list API call sites | Help2day-required | Agent detail heartbeat queries are capped and verified not to request unbounded history. |
| Routine per-tick catch-up quota | Decision: adopt upstream `8368fb30b`. HEAD can still dispatch up to the historical catch-up burst per routine per tick; the upstream coalescing fix is narrower than fork-only rewrites and should be preferred. | Routine scheduler stability | FUL-16652 | `f98d26a4c` fork half; prefer upstream `8368fb30b` | `server/src/services/routines.ts`, routine service tests | Adopt-upstream | Sub-hourly catch-up dispatches are coalesced/capped under tests so one routine cannot burst many stale ticks in one scheduler pass. |
| Terminal-state transition guard | Decision: re-apply. No upstream equivalent was identified in the reviewed commit set; without it, terminal issues can be mutated through invalid state transitions and unblock semantics can widen past done-only. | Issue lifecycle integrity | FUL-16652 | `d624f3d8b`; dropped in `b4ec00b49` | `server/src/services/issues.ts`, issue service tests | Help2day-required | Terminal-state transitions are rejected except for documented recovery paths, and unblock-on-terminal remains done-only where required. |
| Stale prompt closure on terminal issues | Decision: re-apply. No upstream equivalent was identified; pending interactions/prompts can remain open after an issue reaches a terminal state, causing stale approvals and audit confusion. | Review and approval hygiene | FUL-16652 | `9a362c596`; dropped in `b4ec00b49` | `server/src/services/issues.ts`, issue-thread interaction closure | Help2day-required | Terminal issue settlement closes or expires stale prompts with tests and audit events. |
| Reason-keyed wakeup dedup | Decision: re-apply. HEAD keys wakeup dedup without the wake reason, so distinct wakes for the same issue can collapse and lose `issue_blockers_resolved` semantics. | Scheduler correctness | FUL-16652 | `2b7620b85`; regressed again after `b4ec00b49` | heartbeat/wakeup dedup map key in issue routes or scheduler path | Upstreamable | Wakeup dedup includes reason in the key and preserves separate semantic wake reasons for the same issue. |
| Issue marker endpoint removal cleanup | Decision: retire the deleted endpoint completely by removing shared marker schema exports, unless a watcher POST path is still found. The route was removed but `addIssueMarkerSchema` and `ISSUE_MARKER_KINDS` remain exported as dead API surface. | API contract hygiene | FUL-16652 | `8942ba32c`; endpoint deleted during `b4ec00b49` reconciliation | `packages/shared/src/validators/issue.ts`, shared indexes, any marker watcher clients | Retire-with-rationale | No active watcher posts issue markers and the stale shared exports/types are removed, or the endpoint is restored with ownership tests. |
| Recovery-owner bypass low-trust boundary | Decision: tighten back to the original low-trust boundary. Current `issues.ts` recovery action resolution allows the active recovery owner bypass path before the normal mutation check, widening authority beyond the intended reviewer/assignee boundary. | Authorization safety | FUL-16652 | Regression observed after `b4ec00b49`; exact fork commit to recover during implementation | `server/src/routes/issues.ts` recovery action resolution | Security-required | Recovery-owner bypass is limited to the intended low-trust action scope, with negative route tests for unrelated issue mutation. |
| Foreign ZodError handling | Decision: re-apply. HEAD currently maps `err instanceof ZodError` to HTTP 400, but this remains in the register because the reconcile audit flagged a reverted path and future syncs must preserve the foreign-Zod guard, not just same-package `instanceof` handling. | API reliability and client error hygiene | FUL-16652 | Regressed in `b4ec00b49`; compare prior fork Zod guard during implementation | `server/src/middleware/error-handler.ts` | Upstreamable | Zod validation failures from both local and foreign Zod instances return 400 with bounded details instead of 500. |
| Interaction resolution method audit plumbing | Decision: re-apply. `issue-thread-interactions.ts` has a `resolutionMethod` input, but reconciliation dropped end-to-end plumbing so new interaction resolutions can log `unknown`, weakening audit filters. | Approval auditability | FUL-16652 | Dropped in `b4ec00b49`; recover prior resolution-method plumbing | issue-thread interaction routes/services and audit event payloads | Help2day-required | UI, API explicit, and automated interaction resolutions persist and expose the correct method instead of `unknown`. |

## Update contract

For every reconciliation:

1. Compare this register with the exact upstream range and candidate range.
2. Replace abbreviated commit references with the final governed commit set in the release evidence.
3. Add newly discovered divergences before source approval.
4. Mark an entry retired only when the replacement is present, tested, and linked to review evidence.
5. Do not use this file to waive a security, publication, or runtime gate.
